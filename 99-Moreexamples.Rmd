```{r more examples, include=FALSE, message = FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
# More exercises

## Getting data from a package like MDSR
```{r}
library(tidyverse)
library(mdsr)
x <- data(package = "mdsr")
x <- x$results |> 
    as_tibble() |> 
    select(Item, Title)
```
